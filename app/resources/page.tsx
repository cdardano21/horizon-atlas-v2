import ResourceLinks from "../components/ResourceLinks";

export const metadata = {
  title: "Relocation Research Resources | DestinationFinderAI",
  description: "A curated research index for visa, tax, healthcare, housing, safety, mobility, and local destination planning.",
};

export default function ResourcesPage() {
  return <ResourceLinks />;
}